from config import valid_credentials


class DataContainer:
    verification_text = 'Add your first hub to start managing the security system'

    credentials = [valid_credentials, [valid_credentials[0], 'password'], ['email@gmail.com', valid_credentials[1]],
                   ['email@gmail.com', 'password']]
